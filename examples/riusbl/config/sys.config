[{kernel, [{logger_level, info},
           {logger, [{handler, default, logger_std_h, #{level => warning}}]}]},

 {sasl, []},

 {evins, [{fabric_config, "etc/fsm.conf"},
          {user_config, "etc/fsm-user.conf"},
          {log_output, off}]}].
